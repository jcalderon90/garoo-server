services:
  - type: web
    name: garoo-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        fromDatabase:
          name: garoo-db
          property: connectionString
    plan: free
